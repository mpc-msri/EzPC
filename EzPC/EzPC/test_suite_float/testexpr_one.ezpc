def void main () {
  input(SERVER, x, uint64_al[2][3]) ;
  input(CLIENT, y, uint64_al[2][3]) ;

  uint64_al acc = 0uL ;
  for i=[0:2] {
    for j=[0:3] {
      acc = acc + (y[i][j] - x[i][j]) ;
    } ;
  } ;
  output(ALL, acc) ;
}
