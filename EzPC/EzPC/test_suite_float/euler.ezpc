def void main() {
    input(ALL, terms, int32_pl) ;
    float_pl euler = 1.0 ;
    float_pl fact = 1.0 ;
    float_pl ctr = 1.0 ;
    for i=[1:terms] {
        fact = fact * ctr ;
        euler = euler + (1.0/fact) ;
        ctr = ctr + 1.0 ;
    };
    output(ALL, euler) ;
}
