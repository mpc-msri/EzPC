def void main() {
    input(ALL, terms, int32_pl) ;
    float_al euler = 1.0 ;
    float_al fact = 1.0 ;
    float_al ctr = 1.0 ;
    for i=[1:terms] {
        fact = fact * ctr ;
        euler = euler + (1.0/fact) ;
        ctr = ctr + 1.0 ;
    };
    output(ALL, euler) ;
}
